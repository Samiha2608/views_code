<h1>Admin Dashboard- All Post</h1>
<% @posts.each do |post|%>
<h2><%= post.title %></h2>
<p><%= post.body%></p>
<%= link_to "Edit", edit_admin_post_path(post) %> |
<%= button_to "Delete", admin_post_path(post), method: :delete %>
<% end %>
